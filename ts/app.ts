import calculateRectangle from './rectangle';
import calculateCircumference from './circle';

console.log( calculateRectangle(10) );
console.log( calculateRectangle(10, 20) );
console.log( calculateCircumference(10) );

